session_name: odom
windows:
  - window_name: odom
    layout: even-horizontal
    shell_command_before:
      - source devel/setup.bash
    panes:
      - shell_command:
          - roslaunch test rtk.launch 
      - shell_command:
          - sleep 2
          - roslaunch mesh mesh.launch
